@model IEnumerable<Capa_Entidaddes.Puesto>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Nuevo Puesto", "Crear")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ID_Puesto)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Rango)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Saldo_Minimo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Saldo_Maximo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ID_Puesto)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Rango)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Saldo_Minimo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Saldo_Maximo)
        </td>
        <td>
            @Html.ActionLink("Edit", "Editar", new { id=item.ID_Puesto  }) |
            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "Eliminar", new {  id=item.ID_Puesto  })
        </td>
    </tr>
}

</table>
